{
  "name": "frontend-projects",
  "private": true,
  "workspaces": [
    "packages/*",
    "sites/*"
  ],
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "next-transpile-modules": "^9.0.0",
    "turbo": "^1.1.2"
  },
  "scripts": {
    "site1:build": "turbo run build --scope=site1 --include-dependencies"
  }
}
